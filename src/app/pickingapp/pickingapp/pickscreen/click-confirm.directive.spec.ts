import { ClickConfirmDirective } from './click-confirm.directive';

describe('ClickConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
